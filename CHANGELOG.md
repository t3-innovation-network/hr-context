# hr-context ChangeLog

## 0.2.0 - 2026-06-15

- Add `inferredSkill` term to the `SkillClaim` scoped context and `model` term
  to the v1 context, for LLM-extracted skill provenance.
- Add `IInferredSkill` interface and `ISkillClaim.inferredSkill` property.

## 0.1.0

Created initial context.





